export function generateStaticParams() {
  return [{ classGroupId: "class-demo" }];
}

export default function TeacherAttendanceTakeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
