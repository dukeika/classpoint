import PlaceholderPage from "../../../components/placeholder-page";

export default function AuditPage() {
  return (
    <PlaceholderPage
      title="Audit log"
      description="Review sensitive actions across fees, roles, and results."
    />
  );
}
